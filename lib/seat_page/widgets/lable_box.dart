import 'package:flutter/material.dart';

class LableBox extends StatelessWidget {
  const LableBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('LableBox');
  }
}
